<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="sdlDialogStyle">@style/DialogStyleLight.Custom</item>
        <item name="android:actionBarStyle">@style/ActionBar</item>
    </style>

    <style name="StyledButton">
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_margin">@dimen/spacing</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:shadowColor">@android:color/black</item>
        <item name="android:shadowDx">1</item>
        <item name="android:shadowDy">1</item>
        <item name="android:shadowRadius">2</item>
    </style>

    <!-- style simple dialog color -->
    <style name="DialogStyleLight.Custom">
        <!-- anything can be left out: -->
        <item name="titleTextColor">@color/background_cjay</item>
        <item name="titleSeparatorColor">@color/background_cjay</item>
        <item name="messageTextColor">@android:color/black</item>
        <item name="buttonTextColor">@android:color/black</item>
        <item name="buttonSeparatorColor">@android:color/white</item>
        <item name="buttonBackgroundColorNormal">@android:color/white</item>
        <item name="buttonBackgroundColorPressed">@android:color/white</item>
        <item name="buttonBackgroundColorFocused">@android:color/white</item>
        <item name="dialogBackground">@android:color/white</item>
    </style>

    <!-- style actionbar -->
    <style name="ActionBar" parent="@android:style/Widget.Holo.Light.ActionBar.Solid.Inverse">
        <item name="android:backgroundStacked">@color/tab_background_stacked</item>
        <item name="android:background">@color/actionbar_background</item>
        <item name="android:backgroundSplit">@android:color/white</item>
    </style>

    <style name="IssueReportEditText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">10dp</item>
        <item name="android:layout_marginLeft">15dp</item>
        <item name="android:layout_marginRight">15dp</item>
        <item name="android:inputType">textNoSuggestions</item>
        <item name="android:selectAllOnFocus">true</item>
        <item name="android:background">@drawable/border_gray_singleline</item>
    </style>

    <style name="IssueReportList">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_marginLeft">15dp</item>
        <item name="android:layout_marginRight">15dp</item>
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:scrollbars">none</item>
        <item name="android:choiceMode">singleChoice</item>
        <item name="android:minHeight">30dp</item>
    </style>

    <style name="IssueReportLocationCodeEditText">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_marginLeft">10dp</item>
        <item name="android:layout_marginRight">10dp</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">40sp</item>
        <item name="android:textAppearance">?android:attr/textAppearanceLarge</item>
        <item name="android:background">@drawable/textview_location_background</item>
    </style>

    <style name="IssueReportLocationCodeButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_marginLeft">2dp</item>
        <item name="android:layout_marginRight">2dp</item>
        <item name="android:text">"X"</item> <!-- a place holder, actual text assigned at runtime -->
    </style>

</resources>
